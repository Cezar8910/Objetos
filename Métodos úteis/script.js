/*
Object.values -> retorna apenas os valores
Object.entries -> retorna as chaves e os valores do objeto dentro de um array
Object.assign(des, any) -> Parecido com o spread operator, copia o objeto
Object.getOwnPropertyDescriptor(objeto, 'propriedade')
... (spread operator) -> copia o objeto original 
-> Já vistos
Object.keys(retorna as chaves)
Object.freeze(congela o objeto)
Object.defineProperties(define várias propriedades)
Object.defineProperty (define uma propriedade)
*/
